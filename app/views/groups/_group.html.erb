<div id="<%= dom_id group %>">
  <h3 class="text-center">
    <strong><%= t(".group")%>:</strong>
    <%= group.name %>
  </h3>

  <div class="row">
    <% group.schedules.each do |schedule| %>
      <%= render schedule %>
    <% end %>
  </div>
</div>
